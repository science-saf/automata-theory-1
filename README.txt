Small Web site server written in Go.

- Golang code is in 'src/server' directory.
- download & build scripts are in 'scripts' directory.
- javascript, CSS and HTML templates are in 'site-content' directory.
- executable file which runs server appears in 'bin' directory after rebuild.